name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
    paths-ignore:
      - README.md
      - DEPLOYMENT.md
      - deploy.ps1
      # - .github/workflows/**
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"
          
      - name: Install dependencies
        run: npm ci
        
      - name: Type check
        run: npm run type-check
        
      - name: Lint
        run: npm run lint
        
      - name: Run tests
        run: npm run test:coverage
        
      - name: Security audit
        run: npm run security:audit
        continue-on-error: true

  build:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build for production
        run: npm run build:gh-pages
        env:
          NODE_ENV: production
          # GitHub Pages base path - repository name (e.g., '/portfolio/')
          # This ensures assets are loaded from the correct path on GitHub Pages
          VITE_BASE_PATH: /${{ github.event.repository.name }}/
          # n8n Webhook Integration - Required for contact form
          # These secrets must be configured in GitHub repository settings:
          # Settings > Secrets and variables > Actions > New repository secret
          # Add: VITE_N8N_WEBHOOK_URL and VITE_N8N_AUTH_TOKEN
          VITE_N8N_WEBHOOK_URL: ${{ secrets.VITE_N8N_WEBHOOK_URL }}
          VITE_N8N_AUTH_TOKEN: ${{ secrets.VITE_N8N_AUTH_TOKEN }}
          
      - name: Verify base path in build
        run: |
          echo "🔍 Verifying base path in built index.html..."
          if grep -q "/${{ github.event.repository.name }}/assets" dist/index.html; then
            echo "✅ Base path correctly applied in index.html"
          else
            echo "❌ Base path NOT found in index.html!"
            echo "📄 First 50 lines of index.html:"
            head -50 dist/index.html
            echo ""
            echo "🔍 Searching for script tags:"
            grep -i "script.*src" dist/index.html || echo "No script tags found!"
            exit 1
          fi
          
      - name: Copy 404.html to dist
        run: cp 404.html dist/
        
      - name: Verify dist contents
        run: |
          echo "📁 Dist folder contents:"
          ls -la dist/
          echo ""
          echo "📦 Assets folder contents:"
          ls -la dist/assets/
          echo ""
          echo "🖼️ Images folder contents:"
          ls -la dist/img/
          echo ""
          echo "📊 Data folder contents:"
          ls -la dist/data/
          echo ""
          echo "📄 Root files:"
          ls -la dist/*.html dist/*.xml dist/*.txt dist/*.headers 2>/dev/null || true
          echo ""
          echo "🔍 Script tags in index.html:"
          grep -i "script.*src" dist/index.html || echo "No script tags found!"
          
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./dist"

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        
      - name: Deployment status
        run: |
          echo "?? Deployment successful!"
          echo "?? Your site is now available at: ${{ steps.deployment.outputs.page_url }}"
          echo "?? Deployed at: $(date)"
          echo "?? Commit: ${{ github.sha }}"
          echo ""
          echo "?? Deployed files:"
          echo "- index.html ?"
          echo "- assets/ (JS, CSS, fonts) ?"
          echo "- img/ (profile, background) ?"
          echo "- data/ (portfolio data) ?"
          echo "- 404.html (SPA routing) ?"
          echo "- sitemap.xml ?"
          echo "- robots.txt ?"
          echo "- _headers (security) ?"
          echo ""
          echo "?? Component System:"
          echo "- Design tokens ?"
          echo "- UI components ?"
          echo "- Optimized build ?"
