/** Primary brand mark under `public/` (no leading slash). */
export const BRAND_LOGO_PUBLIC_PATH = 'img/brand/logo_landscape_circle.png' as const

/** Relative src for static assets in components (`public/` at site root). */
export const BRAND_LOGO_SRC = `./${BRAND_LOGO_PUBLIC_PATH}` as const
