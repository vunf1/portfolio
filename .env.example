# Environment Variables Template
# 
# SECURITY WARNING: Never commit files with real values (.env.local, .env.production, etc.)
# Copy this file to .env.local and fill in your actual values
# 
# For production builds, create .env.production with your production values
# For GitHub Actions CI/CD, use GitHub Secrets (see README.md)

# ============================================
# n8n Webhook Integration
# ============================================
# REQUIRED: Your n8n webhook URL for contact form submissions
# Example: https://n8n.jmsit.cloud/webhook-test/your-webhook-id
VITE_N8N_WEBHOOK_URL=https://n8n.jmsit.cloud/webhook-test/your-webhook-id

# REQUIRED: Your n8n authentication token
# This token is sent as the X-API-Key header (default) or Authorization header
# Supports both VITE_N8N_AUTH_TOKEN (new) and VITE_N8N_JWT_TOKEN (legacy)
VITE_N8N_AUTH_TOKEN=your-auth-token-here
